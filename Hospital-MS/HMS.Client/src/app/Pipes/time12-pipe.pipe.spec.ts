import { Time12PipePipe } from './time12-pipe.pipe';

describe('Time12PipePipe', () => {
  it('create an instance', () => {
    const pipe = new Time12PipePipe();
    expect(pipe).toBeTruthy();
  });
});
